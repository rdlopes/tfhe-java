{{javadoc this "try_encrypt_with_client_key"}}
public static {{className}} encrypt({{dataClass.simpleName}} clearValue, ClientKey clientKey) {
  logger.trace("encrypt - clearValue: {}, clientKey: {}", clearValue, clientKey);
  {{className}} encrypted = new {{className}}();
  {{#if valueClass}}
    {{valueClass.simpleName}} value = {{valueClass.simpleName}}.valueOf(clearValue);
    execute(() ->{{nativeMethod this
                                "try_encrypt_with_client_key"}}(value.getAddress(), clientKey.getValue(), encrypted.getAddress()))
    ;
  {{else}}
    execute(() ->{{nativeMethod this
                                "try_encrypt_with_client_key"}}(clearValue, clientKey.getValue(), encrypted.getAddress()))
    ;
  {{/if}}
  return encrypted;
}
