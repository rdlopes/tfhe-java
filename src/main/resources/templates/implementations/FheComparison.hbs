{{javadoc this "lt"}}
public FheBool lt({{className}} other) {
  logger.trace("lt - other: {}", other);
  FheBool result = new FheBool();
  execute(() -> {{nativeMethod this "lt"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "le"}}
public FheBool le({{className}} other) {
  logger.trace("le - other: {}", other);
  FheBool result = new FheBool();
  execute(() -> {{nativeMethod this "le"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "gt"}}
public FheBool gt({{className}} other) {
  logger.trace("gt - other: {}", other);
  FheBool result = new FheBool();
  execute(() -> {{nativeMethod this "gt"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "ge"}}
public FheBool ge({{className}} other) {
  logger.trace("ge - other: {}", other);
  FheBool result = new FheBool();
  execute(() -> {{nativeMethod this "ge"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "min"}}
public {{className}} min({{className}} other) {
  logger.trace("min - other: {}", other);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "min"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "max"}}
public {{className}} max({{className}} other) {
  logger.trace("max - other: {}", other);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "max"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}
