{{javadoc this "shl"}}
public {{className}} shiftLeft({{className}} other) {
  logger.trace("shiftLeft - other: {}", other);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "shl"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "shr"}}
public {{className}} shiftRight({{className}} other) {
  logger.trace("shiftRight - other: {}", other);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "shr"}}(getValue(), other.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "rotate_left"}}
public {{className}} rotateLeft({{className}} positions) {
  logger.trace("rotateLeft - positions: {}", positions);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "rotate_left"}}(getValue(), positions.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "rotate_right"}}
public {{className}} rotateRight({{className}} positions) {
  logger.trace("rotateRight - positions: {}", positions);
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "rotate_right"}}(getValue(), positions.getValue(), result.getAddress()));
  return result;
}

{{javadoc this "leading_ones"}}
public {{className}} leadingOnes() {
  logger.trace("leadingOnes");
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "leading_ones"}}(getValue(), result.getAddress()));
  return result;
}

{{javadoc this "leading_zeros"}}
public {{className}} leadingZeros() {
  logger.trace("leadingZeros");
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "leading_zeros"}}(getValue(), result.getAddress()));
  return result;
}

{{javadoc this "trailing_ones"}}
public {{className}} trailingOnes() {
  logger.trace("trailingOnes");
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "trailing_ones"}}(getValue(), result.getAddress()));
  return result;
}

{{javadoc this "trailing_zeros"}}
public {{className}} trailingZeros() {
  logger.trace("trailingZeros");
  {{className}} result = new {{className}}();
  execute(() -> {{nativeMethod this "trailing_zeros"}}(getValue(), result.getAddress()));
  return result;
}
