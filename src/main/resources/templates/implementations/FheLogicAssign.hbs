{{javadoc this "bitand_assign"}}
public void bitAndAssign({{className}} other) {
  logger.trace("bitAndAssign - other: {}", other);
  execute(() -> {{nativeMethod this "bitand_assign"}}(getValue(), other.getValue()));
}

{{javadoc this "bitor_assign"}}
public void bitOrAssign({{className}} other) {
  logger.trace("bitOrAssign - other: {}", other);
  execute(() -> {{nativeMethod this "bitor_assign"}}(getValue(), other.getValue()));
}

{{javadoc this "bitxor_assign"}}
public void bitXorAssign({{className}} other) {
  logger.trace("bitXorAssign - other: {}", other);
  execute(() -> {{nativeMethod this "bitxor_assign"}}(getValue(), other.getValue()));
}
