package {{packageName}};

import io.github.rdlopes.tfhe.api.keys.*;
import io.github.rdlopes.tfhe.ffm.TfheHeader;
import io.github.rdlopes.tfhe.internal.data.*;
import io.github.rdlopes.tfhe.internal.serde.*;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
  {{#each imports}}
  import {{this}};
  {{/each}}

import static io.github.rdlopes.tfhe.ffm.NativeCall.*;
import static io.github.rdlopes.tfhe.ffm.NativePointer.*;
import static io.github.rdlopes.tfhe.ffm.TfheHeader.*;

public class {{className}}
  {{#if superClass}}extends {{superClass.simpleName}}{{/if}}
  {{#if interfaces}}implements {{#each interfaces}}{{this.simpleName}}{{#if @last}}{{else}}, {{/if}}{{/each}}{{/if}} {
private static final Logger logger = LoggerFactory.getLogger({{className}}.
class);

  {{javadoc this "destroy" indent="2"}}
  {{className}} (){
  logger.

trace("init");
    super(TfheHeader::{{nativeMethod this "destroy"}});
  }

  {{#each interfaces}}
    {{implement this indent="2"}}
  {{/each}}

  }
