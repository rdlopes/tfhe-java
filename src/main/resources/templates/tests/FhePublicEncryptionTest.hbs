@Test
  {{#if tested.highBitSize}}
  @Tag("intensive"){{/if}}
@Tag("FhePublicEncryption")
void encryptionWithPublicKey() {
  logger.trace("encryptionWithPublicKey");

  PublicKey publicKey = PublicKey.createFromClientKey(keySet.clientKey());
  {{testValue tested}}
  {{tested.className}} encrypted = {{tested.className}}.encrypt(originalValue, publicKey);
  {{tested.dataClass.simpleName}} decrypted = encrypted.decrypt(keySet.clientKey());

  assertThat(decrypted).isEqualTo(originalValue);
}
