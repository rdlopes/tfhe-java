{{javadoc "destroy"}}
{{className}}(){
  {{trace "init"}}
  super(TfheHeader::{{symbol "destroy"}});
  }

{{#method this (stringFormat "public %s" typeName) "decompress" "decompress"}}
  {{declare "decompressed" class=typeName}}
  {{execute "decompress" "getValue(), decompressed.getAddress()"}}
    return decompressed;
{{/method}}

{{> traits/FheSerialization}}
